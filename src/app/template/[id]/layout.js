export const metadata = {
  title: "Resume Builder",
  description:
    "Build your resume with AchiVAI's online editor — add experience, education, skills, projects, and export to PDF.",
};

export default function EditorLayout({ children }) {
  return children;
}