export const metadata = {
  title: "Sign In — AchiVAI",
  description:
    "Sign in to AchiVAI with Google or email to save your resumes, run AI analysis, and download ATS-friendly PDFs.",
  alternates: { canonical: "/login" },
  robots: { index: true, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}